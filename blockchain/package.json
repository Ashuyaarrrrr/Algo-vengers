{
  "name": "herbchain-blockchain",
  "version": "1.0.0",
  "description": "HerbChain smart contracts - Hardhat environment",
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "fund": "hardhat run scripts/fund.js --network localhost",
    "demo": "hardhat run scripts/demo-lifecycle.js --network localhost",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^16.6.1"
  }
}
